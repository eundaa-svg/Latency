# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.

# Password for /admin — set something strong
ADMIN_PASSWORD=your-password-here

# Set to "true" to enable the admin panel (recommended: true locally, false on Vercel)
ADMIN_ENABLED=true
